// Figures for module 2-5 — N-Body Gravity.
// Schema: { '<taskNum>': [{ slug, caption, alt?, placement: 'intro' | 'goal' }] }
// Markup lives at ./2-5/<taskNum>-<slug>.svg (path derived by figures/index.js).

export default {
  '2': [
    {
      slug: 'all-pulls',
      caption: 'sixty-three pulls per body, summed in one thread — n² work, n time',
      placement: 'intro',
    },
  ],
  '3': [
    {
      slug: 'softening',
      caption: 'close encounters flatten out instead of blowing up',
      placement: 'intro',
    },
  ],
};
