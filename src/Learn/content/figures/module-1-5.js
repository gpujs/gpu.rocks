// Figures for module 1-5 — Measuring Speed Honestly.
// Schema: { '<taskNum>': [{ slug, caption, alt?, placement: 'intro' | 'goal' }] }
// Markup lives at ./1-5/<taskNum>-<slug>.svg (path derived by figures/index.js).

export default {
  '1': [
    {
      slug: 'warm-up',
      caption: 'the first call buys the compiler — time the calls after it',
      placement: 'intro',
    },
  ],
  '2': [
    {
      slug: 'transfer-tax',
      caption: 'same +1 either way — the bill tracks bytes, not math',
      placement: 'intro',
    },
  ],
};
